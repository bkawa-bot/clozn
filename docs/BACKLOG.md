# Clozn — Backlog (retired 2026-07-20)

This document was the ordered open-work tracker from 2026-07-16 to 2026-07-20. It is retired:
**ordering and open work now live in [`docs/PRODUCT_ROADMAP.md`](PRODUCT_ROADMAP.md)**, which was
synthesized from this backlog, the research ledger (`docs/RESEARCH_ROADMAP.md`), and the two
2026-07-20 positioning audits. Retiring it follows this file's own philosophy — shipped work
lives in git history — now applied to the tracker itself, before staleness could mislead (two
entries had already gone stale within days).

Where things went:

- **Every open item → a roadmap phase or research lane.** The full item-by-item map is
  `PRODUCT_ROADMAP.md` §12 (item numbers cited elsewhere in the repo refer to that map).
- **Binding honesty invariants** → `PRODUCT_ROADMAP.md` §3 (Gate 0) + `notes/CLOZN_SOUL.md`.
- **Item 9's tracer working detail** (remaining sub-items, traps, headline numbers) →
  `scripts/tracer/README.md`.
- **Item 10's risk-controller autopsy** (the full gate result, numbers, artifact paths) →
  `docs/RESEARCH_ROADMAP.md`, Killed list.
- **Engine debt + the VRAM execution constraint** → `PRODUCT_ROADMAP.md` §8 tail.
- **The done-archive** (Phase-1/2 verifications, commits, measured bars) → git history:
  `git log --follow docs/BACKLOG.md`, or `git show 9818817:docs/BACKLOG.md` for the last full
  revision.
