# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv*/
venv/
build/
dist/

# --- Tooling caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- C++ / CMake out-of-source build trees (incl. the CUDA DLLs) ---
/engine/**/build-*/
/kernels/**/build-*/

# --- Vendored llama.cpp: pinned at e36a602 + the CLOZE patch (engine/core/third_party/patches/).
# Not tracked here (link, don't fork). Proper git submodule is a follow-up (needs a network clone);
# until then, recreate with: clone llama.cpp @ e36a602, apply 0001-llama_get_logits_tensor.patch. ---
/engine/core/third_party/llama.cpp/

# --- Model weights — multi-GB GGUFs / checkpoints live locally, never in the repo ---
/engine/**/models/
*.gguf
*.safetensors
*.pt

# --- Generated demo outputs (inspector/research write HTML/png into runs/) ---
runs/

# --- Local run/scratch artifacts ---
*.log
req_*.json

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Claude Code local harness state ---
.claude/

# --- Local-only context for AI tooling / cross-project notes — kept out of the public repo ---
# CLAUDE.md (no leading slash) matches at ANY depth (e.g. research/CLAUDE.md), not just the root.
CLAUDE.md
/PROJECT_STATUS.md
/notes/
