# Phase-0 spine + spike: numpy only (runs anywhere).
numpy>=1.24

# Phase-1 recurrent inspector (M1-M4) — the REAL adapter, no Triton needed:
torch>=2.1
transformers>=4.40                    # HuggingFace RWKV-4 exposes the explicit recurrent state

# Phase-1 flagship adapter (fla_rwkv.py, stubbed) — when on WSL/Linux:
#   flash-linear-attention            # RWKV-7 / Gated DeltaNet (Triton; dicey on Windows)
# Later (SAE feature atlas):
#   sae-lens                          # cleanest on Gemma Scope 2 models
